# MVP verification — 16 September 2026

Integration based on Ray's main `292592e`, including his latest charts, table controls and interactive map. Tested on localhost:3101 against the dedicated project database, using Jochem's registered and confirmed officer account.

Passed: registration and email confirmation (user completed password entry); pending access before administrator grant; authenticated dashboard; 543 real establishments and 27 Paalstraat results; business-name and establishment-ID search; empty search; correct Amplifon establishment/parent separation; dated evidence and working external source link; phone approval and email rejection; server-recorded reviewer identity; full page reload preserves both decisions; downloaded CSV has only the approved phone and its source URL; real coordinate map; source inventory; supported source refresh preserves reviewed proposals and adds an opening-hours proposal; sign-out and protected-page redirect afterward.

Latest Ray interface also verified in Brave: charts show real totals, command search opens the canonical Amplifon dossier, table pagination advances, and the interactive map renders 525 supplied locations with 18 unlocated records retained in the list. Map search and selected-dossier evidence/review states pass.

Backend checks: all protected APIs return 401 when signed out; foreign-origin writes return 403. Actual persisted dossier inspection confirmed both reviews, correct actor ID, independent proposal revisions and preserved evidence. 19 regression tests, lint, all relevant TypeScript checks and production build pass locally.

Test decisions intentionally remain in the Amplifon history with explicit MVP test notes. The phone is supported by two local sources; the email rejection tests export filtering and is not a claim that the address is wrong. They can be superseded through a new review, preserving the audit. The opening-hours proposal remains pending for the demo.

Limitations: partial sample, no municipal completeness claim; 543 real establishments are loaded in one bounded workspace request; source refresh has explicitly configured targets only; map coordinates are range-checked, not field-verified; the Toestanden page is a UI-state preview. No hosted deployment or official register publication was performed. New users require confirmed email and a trusted officer-role grant. AI budget uses one local shared ledger and is not suitable for multiple independent hosts/serverless instances without a shared atomic reservation store.

## Ray's local checkout verification — 16 September 2026

Merged `origin/main` at `01d61316afd6e708cf740e1d1954b893540ef8c6` into `codex/restore-dashboard-integration`. Pending design and legacy AI work was first committed on `codex/safety-before-backend-integration` (`65dfc81`). The integration branch restores the detailed dashboard and compact typography, retaining canonical backend mappings and the map retry improvement. The separate legacy AI work remains recoverable from the safety commit and is not enabled in the integrated app.

Local fixes: map requests now use the shared same-origin `/api` helper and cannot be redirected to obsolete port 3001 by `NEXT_PUBLIC_API_URL`; canonical error messages and abort signals are retained. The scripts directory has an explicit ESM package boundary so `npm run db:check` runs on this Windows/Node setup.

Passed locally: `npm ci`, `npm run db:check`, `npm run check` (21 tests, lint and TypeScript), and `npm run build`. Existing local environment preserved and completed for the canonical project. No credentials are committed. `OPENAI_API_KEY` and `AI_BUDGET_FILE` remain unset; no AI refresh, budget initialization, migration, seed or legacy API server was run.

Browser verification on `http://localhost:3000` after the owner entered their password: exact confirmed account received a server-managed officer role; dashboard shows 543 establishments and street charts; Paalstraat table shows 27; pagination advances; Amplifon search opens the local Paalstraat 38 record and independently shows parent Alfons Gossetlaan 40 in Dilbeek, source URLs and retrieval dates. Map renders 525 locations, retains 18 unlocated records, searches and zooms to Amplifon, and displays canonical evidence and review states. Three existing review events survive a full page reload. Signing out returns to login, and direct navigation to the protected dashboard remains blocked. Signed-out workspace, locations and export API requests return 401; protected pages redirect to login.

Read-only database verification validates all 543 real dossiers against the canonical contract and confirms 27 Paalstraat records, Amplifon's 14 evidence items, source dates, approved telephone and rejected email. The canonical approved CSV generator yields exactly one approved telephone row. The in-app browser export button was exercised, but its download event/file could not be captured by the browser tool; fresh downloaded-file verification remains outstanding. No new review decisions were written during this local verification.
